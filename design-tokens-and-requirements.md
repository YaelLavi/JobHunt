# מסמך עיצוב ודרישות לבניית האתר — מדריך חיפוש עבודה

מסמך זה מסכם את כל ההחלטות העיצוביות שסגרנו, כדי שנוכל לעבור משלב התצוגות המקדימות לבניית האתר בפועל ברגע שהתוכן המעודכן יהיה מוכן.

## 1. מבנה כללי

* אתר בעברית, כיוון RTL (`dir="rtl"`).
* רספונסיבי לחלוטין — מותאם למחשב ולנייד.
* ללא מערכת זיכרון/caching וללא ניהול משתמשים. האתר לא שומר אילו שלבים נצפו או הושלמו.
* פונט: David Libre.
  קישור גוגל פונטס: `https://fonts.googleapis.com/css2?family=David+Libre:wght@400;500;700&display=swap`

## 2. פריסה (Layout)

* **מחשב:** תפריט צד אנכי, מקובץ לפי שלבי המסע (קבוצות כמו "התחלה", "בונים בסיס", "יוצאים לדרך" וכו'). פריט פעיל מודגש בצבע ההדגשה.
* **נייד:** ציר מסע אנכי (timeline). כרטיס התוכן של השלב הנוכחי מוצג למעלה, והציר עם כל השלבים מוצג מתחתיו כניווט משני. השלב הנוכחי מסומן בעיגול מלא עם טבעת הדגשה; שלבים עתידיים מוצגים כעיגולים מתוארים (outline) בלבד, ללא דהייה/שקיפות.
* פריטי התפריט מיוצגים באמצעות **אייקונים** (לא מספור).
* כפתור ניווט "לשלב הבא" בכל מסך שלב.
* אנימציות עדינות למעברים בין שלבים, כדי לתת תחושת design "חלק" (sleek).

## 3. פלטת צבעים (Design Tokens)

צבע הבסיס: `#006666` (הירוק האהוב על יעל). הרמה המלאה שנבנתה ממנו:

```css
--jh-50:  #e6f2f1;
--jh-100: #c7e3e1;
--jh-200: #9bcbc8;
--jh-300: #62a8a4;
--jh-400: #338784;
--jh-500: #006666;
--jh-600: #005252;
--jh-700: #003e3e;
--jh-800: #002b2b;
--jh-900: #001818;
```

### צבע הדגשה (accent / כפתורים) — מאושר סופית

```css
--jh-accent:      #00CC99;
--jh-accent-text: #00251c;  /* טקסט על גבי האקסנט, לקונטרסט */
```

### רקע אזור התוכן — מאושר סופית

חזרה לבחירה המקורית, הבהירה:

```css
--jh-bg: var(--jh-50);  /* #e6f2f1 */
```

(נבדקו גם `#66b2b2` ו-`#b0cbcb` אך הוחלט לחזור לרקע הבהיר המקורי — נקי, קריא, ומבליט את כפתור ההדגשה הירוק ואת צבעי התפריט.)

## 4. שימושי צבע מומלצים

* רקע ראשי: `--jh-50`
* תפריט צד / chrome: `--jh-700` עם טקסט `--jh-100` / `--jh-300`
* פריט תפריט פעיל: רקע `--jh-accent`, טקסט `--jh-accent-text`
* כותרות: `--jh-800`
* טקסט גוף: `--jh-600` / `--jh-300` (לפי הקשר, תמיד גוון מהמשפחה — לא שחור/אפור סתמי)
* כרטיסי תוכן (כמו בנייד): רקע לבן `#ffffff` עם צל עדין, להבדלה מהרקע

## 5. מבנה השלבים (לתפריט וניווט)

רשימת 14 השלבים, בהתאמה לכותרות במסמך, עם אייקון לכל אחד (Tabler icons):

```js
[
  {icon:'ti-mood-smile',        title:'הכנה נפשית'},
  {icon:'ti-target-arrow',      title:'מה אני רוצה?'},
  {icon:'ti-users',             title:'נטוורקינג'},
  {icon:'ti-file-text',         title:'קורות חיים'},
  {icon:'ti-brand-linkedin',    title:'לינקדאין / גלאסדור'},
  {icon:'ti-heart-handshake',   title:'קהילות'},
  {icon:'ti-search',            title:'החיפוש עצמו'},
  {icon:'ti-list-check',        title:'מעקב אחר ההגשות'},
  {icon:'ti-send',              title:'הגשת קו"ח למשרה'},
  {icon:'ti-currency-shekel',   title:'ציפיות שכר'},
  {icon:'ti-phone',             title:'ראיון סינון'},
  {icon:'ti-presentation',      title:'ראיונות ושלבים הבאים'},
  {icon:'ti-confetti',          title:'קיבלתם הצעה?'},
  {icon:'ti-bulb',              title:'אתרים ומקורות מומלצים'}
]
```

הערה: רשימה זו תעודכן בהתאם לקובץ ה-md הסופי שיתקבל (ייתכנו שינויים בכותרות/סדר/תוספות).

## 6. נגישות

* כל מסך/תצוגה כולל כותרת מוסתרת (`sr-only`) המסכמת את התוכן עבור קוראי מסך.
* קונטרסט טקסט-על-רקע: שימוש בגוון הכהה/הבהיר ביותר מאותה משפחת צבעים, לעולם לא שחור/אפור גנרי.
* יעדי לחיצה (tap targets) בגודל מספק במובייל.

## 7. צעדים הבאים

1. קבלת קובץ ה-md המעודכן מיעל.
2. התאמת רשימת השלבים והתוכן לפי הקובץ הסופי.
3. בניית מבנה האתר בפועל (HTML/CSS/JS) לפי הטוקנים והדרישות שלעיל.
4. בדיקות רספונסיביות ונגישות לפני מסירה.
